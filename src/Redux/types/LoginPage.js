export const IS_AUTH = 'IS_AUTH';
export const IS_OPEN = 'IS_OPEN';

export const isAuth = () => {
    return {
        type: IS_AUTH
    }
}

export const isOpen = () => {
    return {
        type: IS_OPEN
    }
}
